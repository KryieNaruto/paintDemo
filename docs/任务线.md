# 任务线 · DGCPaint 原型开发（SOT）

> **这是任务状态与依赖的唯一权威记录。** 状态只由 `.exec/taskline.py` 修改，**不要手改表格**——认领 / 收尾 / 退回都走脚本，脚本会原子更新状态与进度汇总。
>
> 状态枚举：`可申领` `执行中` `阻塞` `已完成`；审核枚举：`待审核` `已通过` `打回`。
> 依赖列 = 顿号分隔的任务 ID，`—` = 无依赖。
> 技术背景 → `../DGCPaint_技术规划.md`；怎么跑一个任务 → `.claude/skills/paint-dev/SKILL.md`。

## 进度

<!-- SUMMARY-BEGIN -->
总 15 · 可申领 15 · 执行中 0 · 阻塞 0 · 已完成 0
<!-- SUMMARY-END -->

## 起点条件（人工，无任务承载）

| 项 | 状态 |
|---|---|
| Android Studio Ladybug+ / NDK r27+ / CMake 3.31 / JDK 21 | 待装 |
| VS2026（C++ 桌面开发 + CMake tools）+ LunarG Vulkan SDK | 待装 |
| 物理测试平板（Galaxy Tab S9+/S10+ 等）+ 开发者模式 / USB 调试 | 待备 |

## 任务表

| ID | 任务线 | 任务名称 | 任务状态 | 依赖 | 审核 |
|---|---|---|---|---|---|
| T0-1 | 线0-spike | Jetpack Ink 预测层剥离验证（InkStroke 仅作输入数据源 + graphics-core 预测独立调用 → 输出「预测复用 vs 降级 A/B」结论） | 可申领 | — | 待审核 |
| T0-2 | 线0-spike | libmypaint 交叉编译链验证（arm64 编译通过 + stroke_to 跑通，走 mypaint_ffi 已验证路径） | 可申领 | — | 待审核 |
| T1-1 | 线1-脚手架 | CMake 双 toolchain 工程骨架（顶层 CMakeLists + CMakePresets + core/mypaint/vulkan/jni 目录 + VS2026 host 配置） | 可申领 | — | 待审核 |
| T1-2 | 线1-脚手架 | Vulkan 初始化 + swapchain + 全屏渐变渲染（instance/device/queue/swapchain，稳定 60fps） | 可申领 | T1-1 | 待审核 |
| T1-3 | 线1-脚手架 | Compose + TextureView 集成（Android 工程 + AndroidView 嵌入 + onSurfaceTextureAvailable 创建 RenderThread） | 可申领 | T1-2 | 待审核 |
| T2-1 | 线2-libmypaint | libmypaint + json-c 交叉编译打包（build_mypaint_android.sh + 产物进 third_party + mypaint-brushes 进 assets） | 可申领 | T0-2 | 待审核 |
| T2-2 | 线2-libmypaint | MyPaintSurface 封装 → StampData（mypaint_surface.cpp 回调产出 StampData） | 可申领 | T2-1 | 待审核 |
| T2-3 | 线2-libmypaint | JNI 桥（jni_bridge.cpp：createBrush/beginStroke/strokeTo/endStroke） | 可申领 | T2-2、T1-1 | 待审核 |
| T3-1 | 线3-compute | Canvas storage image + stamp 纹理池 + staging 上传（vk_canvas + staging buffer 池） | 可申领 | T1-2 | 待审核 |
| T3-2 | 线3-compute | brush_composite.comp + 批量 dispatch + 包围盒（shader 编译 + vk_composite） | 可申领 | T3-1 | 待审核 |
| T3-3 | 线3-compute | present 管线（present.vert/frag 把 Canvas 画到 swapchain） | 可申领 | T3-1、T1-2 | 待审核 |
| T4-1 | 线4-input | Jetpack Ink 点流接入（InkHandler + ring_buffer + 预测点流，按 T0-1 结论） | 可申领 | T0-1、T2-3、T3-2 | 待审核 |
| T4-2 | 线4-input | 预测点覆盖策略（isPredicted 标记 + 真实点重合成） | 可申领 | T4-1 | 待审核 |
| T5-1 | 线5-集成 | 全链路联调 + 压测（大 canvas + 连续快速笔触） | 可申领 | T4-1、T3-3 | 待审核 |
| T5-2 | 线5-集成 | 性能测试（AGI/RenderDoc/高速摄影测 3.3 指标）+ 性能报告 + 技术路线结论 | 可申领 | T5-1 | 待审核 |

## 每条线的测试指标

> 测试指标 = 评审 agent 打 `通过` 的必要条件。来自 `DGCPaint_技术规划.md` 各阶段验收标准 + §3.3 性能指标。**不达标不打「通过」**。

### 线0-spike（T0-1、T0-2）

| 任务 | 指标 | 测量方法 |
|---|---|---|
| T0-1 | ✅ 输出结论文档，明确「预测复用 vs 降级 A/B」选型，含实测调用路径验证 | 结论文档送审 |
| T0-2 | ✅ arm64 交叉编译 libmypaint 通过 | 编译日志 |
| T0-2 | ✅ `stroke_to` 跑通，产出的 dab 数据可验证（形状正确） | 日志 dump / 离屏位图 |

### 线1-脚手架（T1-1、T1-2、T1-3）

| 任务 | 指标 | 测量方法 |
|---|---|---|
| T1-1 | ✅ CMakePresets host/android 两配置一键切换，`cmake --build` 通过 | CMake 构建日志 |
| T1-1 | ✅ 目录结构（core/mypaint/vulkan/jni/shaders）落到位 | `ls` 核对 |
| T1-2 | ✅ 设备上看到 Vulkan 渐变画布 | 肉眼 |
| T1-2 | ✅ 帧率稳定 60fps 运行 ≥30 秒 | AGI / log `dFps` |
| T1-3 | ✅ Compose 中嵌入 TextureView | 肉眼 |
| T1-3 | ✅ `onSurfaceTextureAvailable` 触发 RenderThread 创建，日志可见渲染循环 | logcat |

### 线2-libmypaint（T2-1、T2-2、T2-3）

| 任务 | 指标 | 测量方法 |
|---|---|---|
| T2-1 | ✅ `build_mypaint_android.sh` 产出 `arm64-v8a/libmypaint.a` + `json-c.a`，产物进 `third_party/` | `ls` 核对 |
| T2-1 | ✅ mypaint-brushes 预设打包进 `assets/` | APK 解包验证 |
| T2-2 | ✅ MyPaintSurface 回调产出 StampData（含 x/y/radius/opacity/color/hardness） | 日志 dump 验证 |
| T2-2 | ✅ **单 stamp 耗时 < 3ms** | CPU 打点 (`std::chrono`) |
| T2-3 | ✅ JNI 调用 `createBrush/beginStroke/strokeTo/endStroke` 成功，返回数据可验证 | logcat |

### 线3-compute（T3-1、T3-2、T3-3）

| 任务 | 指标 | 测量方法 |
|---|---|---|
| T3-1 | ✅ Canvas storage image 创建成功（`VK_IMAGE_USAGE_STORAGE_BIT\|SAMPLED_BIT\|TRANSFER_DST_BIT`） | Vulkan 校验层 / 日志 |
| T3-1 | ✅ stamp 纹理池 + staging buffer 上传链路跑通 | 日志 |
| T3-1 | ✅ **单 stamp 上传 < 1ms** | CPU 打点 |
| T3-2 | ✅ `brush_composite.comp` 编译为 SPIR-V | 构建日志 |
| T3-2 | ✅ 批量 dispatch 用固定输入在画布画出笔刷痕迹 | 肉眼 |
| T3-2 | ✅ **单次 dispatch 合成 < 2ms** | Vulkan timestamp query |
| T3-3 | ✅ present 管线把 Canvas 内容画到 swapchain，画面可见 | 肉眼 |

### 线4-input（T4-1、T4-2）

| 任务 | 指标 | 测量方法 |
|---|---|---|
| T4-1 | ✅ InkHandler 接收 MotionEvent → ring_buffer 预测点流 → libmypaint → stamp → Vulkan 合成，完整链路跑通 | logcat 追踪 |
| T4-2 | ✅ 预测 stamp 标记 `isPredicted` | 日志验证 |
| T4-2 | ✅ 真实点到达后重合成覆盖，无拖影残留 | 肉眼 / 高速摄影 |

### 线5-集成（T5-1、T5-2）

| 任务 | 指标 | 测量方法 |
|---|---|---|
| T5-1 | ✅ 大 canvas（≥2048×2048）压测，无崩溃 | 持续绘制 ≥5 分钟 |
| T5-1 | ✅ 连续快速笔触无显存泄露 | AGI 追踪显存 |
| T5-2 | ✅ **端到端延迟 < 30ms** | 高速摄影 / SurfaceFlinger latency |
| T5-2 | ✅ **绘制中帧率稳定 60fps**（120Hz 屏测 120fps） | AGI / Choreographer FrameTimeline |
| T5-2 | ✅ **Compute 合成 < 2ms** | Vulkan timestamp query |
| T5-2 | ✅ **Stamp 上传 < 1ms** | CPU 打点 |
| T5-2 | ✅ **libmypaint 单 stamp < 3ms** | CPU 打点 |
| T5-2 | ✅ 产出性能报告 + 技术路线结论文档 | 文档送审 |

## 里程碑

| 里程碑 | 触发 | 交付 |
|---|---|---|
| M1 | T1-3 已完成 | Vulkan 画布跑通 |
| M2 | T3-3 已完成 | 笔刷经 compute 合成可画 |
| M3 | T5-2 已完成 | 全链路 + 性能报告 |
