# 工作区隔离（git worktree 落点）
.worktrees/

# 状态锁 / 缓存
.exec/.taskline.lock
.exec/__pycache__/
__pycache__/
*.pyc

# 构建产物
build/
build-deps/
.cxx/
*.o
*.a
*.so
.gradle/
local.properties
app/build/

# IDE
.idea/
.vscode/
*.iml
.DS_Store

# 大文件 / 抓帧依据（49MB，不入版控）
RenderDoc结果/*.rdc

# Claude Code 本地个人配置（不入库）
.claude/settings.local.json

# deps 供给产物（拉取/解包大文件，不入版控）
deps/usr/
deps/cache/
